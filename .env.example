# Twingate Configuration
# Get your API key from Twingate console
export TWINGATE_API_KEY=your_twingate_api_key_here

# AAP (Ansible Automation Platform) Configuration
# Token-based authentication (recommended)
export TOWER_HOST=https://your-aap-instance.com
export TOWER_OAUTH_TOKEN=your_aap_token_here
export TOWER_VERIFY_SSL=true

# Alternative: Username/Password authentication
# Uncomment and use if you don't have a token
# TOWER_USERNAME=your_username
# TOWER_PASSWORD=your_password
# unset TOWER_OAUTH_TOKEN

# Jira Configuration (optional)
# Set ONE of the authentication methods AND pass -e "jira_enabled=true" to enable automatic ticket closure
# Note: Jira integration is disabled by default
export JIRA_BASE_URL=https://issues.redhat.com

# Method 1: Personal Access Token (RECOMMENDED for Red Hat Jira / Jira Data Center)
# Only ONE credential needed - simpler and more secure
export JIRA_PAT=your_personal_access_token_here
# To get a PAT for Red Hat Jira:
# 1. Go to https://issues.redhat.com
# 2. Click your profile → Personal Access Tokens
# 3. Click "Create token"
# 4. Copy the token and paste above

# Method 2: Email + API Token (for Jira Cloud or if PAT not available)
# If JIRA_PAT is set, these will be ignored (PAT takes priority)
#export JIRA_EMAIL=your_email@redhat.com
#export JIRA_API_TOKEN=your_jira_api_token_here
# To get an API token for Jira Cloud:
# 1. Go to https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Click "Create API token"
# 3. Copy the token and paste above

# To use Jira integration, run playbooks with:
#   ansible-playbook playbooks/main.yml -e "jira_enabled=true" -e "aap_template_names=..."

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Run with ./run.sh (automatically loads and exports variables)
#
# Or manually:
# 3a. Load into your shell: set -a && source .env && set +a
# 3b. Run playbooks
#
# Note: The run.sh script automatically exports these variables.
# If running playbooks directly, use 'set -a && source .env && set +a' first.
